<h1>Quiz Categories</h1>
<ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.name, category_path(category) %></li>
  <% end %>
</ul>
<%= link_to 'Geography', category_path(Category.find_by(name: 'Geography')), class: 'btn btn-primary' %>
