<h1>Create a New Quiz in <%= @category.name %></h1>
<%= form_with(model: [@category, @quiz], local: true) do |form| %>
  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
